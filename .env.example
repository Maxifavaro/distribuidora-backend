# SQL Server auth (default)
# DB_USER and DB_PASSWORD are provided by environment, defaults are sa/Apex34 for convenience
DB_USER=sa
DB_PASSWORD=Apex34
DB_SERVER=localhost
DB_DATABASE=OMMISYS
DB_PORT=1433

# JWT secret for authentication
JWT_SECRET=your_jwt_secret_here
